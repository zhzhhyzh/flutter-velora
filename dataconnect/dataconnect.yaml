specVersion: "v1"
serviceId: "dvelora"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dvelora-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
